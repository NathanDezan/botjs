class Group {
    constructor(groupNumber, groupID, groupName, members) {
        this.groupNumber = groupNumber;
        this.groupID = groupID;
        this.groupName = groupName;
        this.members = members;
    }
}

module.exports = { Group };